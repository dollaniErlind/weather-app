export enum UnitEnum {
  METRIC = 'metric', // If unit = metric, the response temperature would be in Celsius and wind speed in meter/sec
  IMPERIAL = 'imperial' // If unit = imperial, the response temperature would be in Fahrenheit and wind speed in miles/hour
}
